/* No curso vai ser utilizado o let */
/* utilizado o camelCase */

let nomeDeAlguem = "Robert"

/* Constante é uma variavel que não pode ser mudada, o seu valor permanece. */
/* A constante precisa ser criada e inicializada ao mesmo tempo. */

const nome = 'Gabriel'

const name = prompt("Digite o seu nome completo")